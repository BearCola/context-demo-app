import React, { Component } from "react";

export default class PageContent extends Component {
  render() {
    const styles = {
      backgroundColor: "red",
      height: "100vh",
      width: "100vh"
    };
    return <div />;
  }
}
